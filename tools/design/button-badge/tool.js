const prev=document.querySelector('#badge-preview'),out=document.querySelector('#css-output');function esc(s){return s.replace(/[&<>]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[m]));}function run(){const width=Math.max(90,txt.value.length*10+36);const svg='<svg xmlns="http://www.w3.org/2000/svg" width="'+width+'" height="34"><rect width="'+width+'" height="34" rx="'+r.value+'" fill="'+bg.value+'"/><text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle" fill="'+fg.value+'" font-family="Arial, sans-serif" font-size="15" font-weight="700">'+esc(txt.value)+'</text></svg>';prev.innerHTML=svg;out.textContent=svg;}document.querySelectorAll('input').forEach(i=>i.addEventListener('input',run));run();