let img=null;const c=document.querySelector('#canvas'),ctx=c.getContext('2d');function draw(){if(!img)return;const s=+size.value,w=img.naturalWidth,h=img.naturalHeight;c.width=w;c.height=h;const sw=Math.max(1,Math.floor(w/s)),sh=Math.max(1,Math.floor(h/s));const off=document.createElement('canvas');off.width=sw;off.height=sh;off.getContext('2d').drawImage(img,0,0,sw,sh);ctx.imageSmoothingEnabled=false;ctx.drawImage(off,0,0,sw,sh,0,0,w,h);document.querySelector('#css-output').textContent='Use browser save or right-click to save the pixelated image.';}file.addEventListener('change',()=>{const f=file.files[0];if(!f)return;img=new Image();img.onload=draw;img.src=URL.createObjectURL(f);});size.addEventListener('input',draw);